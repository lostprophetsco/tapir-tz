# 🚀 Деплой на GitHub Pages

## 📋 Настройка автодеплоя

### 1. Включите GitHub Pages
1. Перейдите в ваш репозиторий на GitHub
2. Settings → Pages
3. Source: **GitHub Actions**

### 2. Настройте переменные окружения (если нужно)
1. Settings → Secrets and variables → Actions
2. Добавьте необходимые переменные (например, `API_BASE`)

### 3. Автоматический деплой
- **При push в `main`:** автоматический деплой
- **При Pull Request:** тестовая сборка

## 🛠️ Локальный деплой

### Для тестирования локально:
```bash
# Сборка для продакшена (SSR)
npm run deploy:build

# Генерация статических файлов и превью
npm run deploy:preview
```

### Для ручного деплоя:
```bash
# Сборка SSR приложения
npm run build

# Файлы будут в директории .output/public
```

## ⚙️ Конфигурация

### GitHub Actions Workflow
- **Файл:** `.github/workflows/deploy.yml`
- **Триггеры:** push/PR в main ветку
- **Окружение:** Ubuntu, Node.js 20
- **Разрешения:** Pages, Contents, ID-token
- **SSR:** Включен, сборка для статического хостинга

### Nuxt Конфигурация
- **SSR:** Включен для серверного рендеринга
- **Base URL:** Автоматическая подстановка для GitHub Pages
- **API:** Настроен для работы с внешним API

## 📁 Структура деплоя

```
.output/public/          # Сгенерированные файлы
├── _nuxt/            # JS/CSS ассеты
├── index.html        # Главная страница
└── ...              # Остальные файлы
```

## 🔧 Troubleshooting

### Проблема с путями
Если изображения или стили не загружаются:
1. Проверьте `baseURL` в `nuxt.config.ts`
2. Убедитесь что имя репозитория верно в пути

### API запросы
Для CORS проблем с API:
1. Используйте прокси в Nuxt конфигурации
2. Настройте переменную окружения `NUXT_PUBLIC_API_BASE`

### 404 ошибки
1. Проверьте что `baseURL` настроен правильно
2. Убедитесь что все маршруты работают корректно

### SSR проблемы
1. Проверьте консоль на ошибки гидратации
2. Убедитесь что API запросы работают на сервере

## 🌐 URL после деплоя

После успешного деплоя приложение будет доступно по адресу:
```
https://[username].github.io/tapir-tz/
```

## 📊 Мониторинг

- **GitHub Actions:** Отслеживайте статус сборки
- **GitHub Pages:** Проверяйте статус деплоя
- **Логи:** Детальная информация в Actions logs

## 🔄 Обновление

Любые изменения в `main` ветке автоматически:
1. Запустят CI/CD pipeline
2. Соберут SSR приложение
3. Задеплоят на GitHub Pages

## ⚡ SSR Преимущества

- **SEO:** Оптимизация для поисковиков
- **Performance:** Быстрая загрузка контента
- **User Experience:** Мгновенное отображение
- **Caching:** Эффективное кеширование на сервере
